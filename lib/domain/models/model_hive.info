import 'package:freezed_annotation/freezed_annotation.dart';

part '{$TM_FILENAME_BASE}.freezed.dart';

@freezed
class {$1} with _${$1} implements UICacheModel<{$1}CacheDto> {
  factory {$1}({
    //? add field here
  }) = _{$1};
  const {$1}._();

  @override
  {$1}CacheDto toCacheDto() => {$1}CacheDto(
        //? add field here
      );
}
